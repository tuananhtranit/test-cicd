name: "Manually"

on:
  workflow_dispatch:

jobs:
  job4:
    runs-on: ubuntu-latest
    steps:
      - id: id4
        run: |
          echo "Hello world job 2"
  call-job1:
    needs: [job4]
    uses: tuananhtranit/test-cicd/.github/workflows/wf.yml@main